{% extends 'USECStudentBundle:Default:layout.html.twig' %}

{% block navbar %}
<div class="navbar navbar-inverse">
    <div class="navbar-inner">
        <div class="container">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
			<span class="icon-bar"></span>
          	<span class="icon-bar"></span>
          	<span class="icon-bar"></span>
        </button>
        <div class="nav-collapse collapse">
			<ul class="nav">
				<li><a href="{{ path('homepage') }}#">Accueil</a></li>
				<li><a href="{{ path('subscription_form') }}">Mon profil</a></li>
            	<li><a href="{{ path('studies_available') }}">Les études disponibles</a></li>
            	<li><a href="{{ path('homepage') }}#contact">Contact</a></li>
			</ul>
            {% if app.user is not null %}
			<ul class="nav pull-right">
				    <li><a href="{{ path('logout') }}">{{ app.user.username }} (se déconnecter)</a></li>
			</ul>
			{% endif %}
        </div>
      </div>
    </div>
</div>
{% endblock %}